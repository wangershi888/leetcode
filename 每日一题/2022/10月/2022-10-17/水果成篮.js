/**
 * @param {number[]} fruits
 * @return {number}
 * https://leetcode.cn/problems/fruit-into-baskets/
 */
var totalFruit = function (fruits) {
  /**
   * 给定一个数组，要求选出一个子数组
   * 这个子数组的长度不能超过2， 子数组的内容种类不超过2
   * */
  // 插入一个元素，计算当前子数组的长度
};

let fruits = [1, 2, 1]
console.log(totalFruit(fruits))